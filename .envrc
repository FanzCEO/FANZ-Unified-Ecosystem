# Load mise environment
eval "$(mise activate direnv)"

# Export project-specific variables
export FANZ_ENV="development"
export FANZ_LOG_LEVEL="info"
export FANZ_BRAND="FANZ"

# Load environment variables from .env files
if [ -f .env.local ]; then
  set -a
  source .env.local
  set +a
fi
