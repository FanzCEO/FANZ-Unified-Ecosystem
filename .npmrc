# pnpm configuration
package-lock=false
prefer-frozen-lockfile=false
auto-install-peers=true
registry=https://registry.npmjs.org/
strict-peer-dependencies=false
shamefully-hoist=true

# Skip optional dependencies that may fail
optional=false

# GitHub Package Registry for @fanzceo packages (disabled until NODE_AUTH_TOKEN is set)
# @fanzceo:registry=https://npm.pkg.github.com
# //npm.pkg.github.com/:_authToken=${NODE_AUTH_TOKEN}
