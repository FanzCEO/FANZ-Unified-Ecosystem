# Textures

Add your texture files here:

- `noise.png` - Grain texture overlay (8-15% opacity recommended)
- `scanlines.png` - CRT scanline effect
- Sticker SVGs for brand accents

For now, the site uses CSS gradients for the noise effect. You can replace with actual texture files later.
