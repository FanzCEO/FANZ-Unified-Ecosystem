# PostgreSQL connection string
# 
# For local development:
# DATABASE_URL=postgresql://username:password@localhost:5432/fanzworld
#
# For Neon (cloud PostgreSQL):
# DATABASE_URL=postgresql://username:password@ep-xxx.us-east-1.aws.neon.tech/neondb?sslmode=require
#
# Replace with your actual PostgreSQL database URL
DATABASE_URL=postgresql://your_username:your_password@localhost:5432/fanzworld

# Session Secret - Generate a secure random string for production
# You can generate one using either:
#   openssl rand -base64 32
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=your-secure-session-secret-key-here

# Application Environment
NODE_ENV=development

# Server Port (default: 5000)
PORT=5000

# Optional: Replit Configuration (for Replit deployment)
# REPL_ID=your-repl-id

# Optional: Google Cloud Storage (for file uploads)
# GOOGLE_CLOUD_PROJECT_ID=your-project-id
# GOOGLE_CLOUD_STORAGE_BUCKET=your-bucket-name
